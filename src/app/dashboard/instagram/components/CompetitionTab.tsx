import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function CompetitionTab() {
  return (
    <Card className="mt-6">
      <CardHeader>
        <CardTitle>Inteligência Competitiva</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Componentes desta aba serão implementados a seguir.</p>
      </CardContent>
    </Card>
  );
}
