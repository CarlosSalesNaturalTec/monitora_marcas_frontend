import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function HashtagsTab() {
  return (
    <Card className="mt-6">
      <CardHeader>
        <CardTitle>Radar de Pautas</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Componentes desta aba serão implementados a seguir.</p>
      </CardContent>
    </Card>
  );
}
